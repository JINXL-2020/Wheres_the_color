using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MirrorBlock : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }


    //private void OnTriggerStay2D(Collider2D collision)
    //{
    //    if (collision.tag == "MirrorEnemy" || collision.tag == "MirrorPlayer")
    //    {
    //        Color color = collision.gameObject.GetComponent<SpriteRenderer>().color;
    //        //isCollison = true;
    //        collision.gameObject.GetComponent<SpriteRenderer>().color = new Color(color.r, color.g, color.b, 1);
    //    }

    //}
    //private void OnTriggerExit2D(Collider2D collision)
    //{
    //    if (collision.tag == "MirrorEnemy" || collision.tag == "MirrorPlayer")
    //    {
    //        Color color = collision.gameObject.GetComponent<SpriteRenderer>().color;
    //        //isCollison = true;
    //        collision.gameObject.GetComponent<SpriteRenderer>().color = new Color(color.r, color.g, color.b, 0);
    //    }

    //}
}
